<div class="main">
  <div class="card devise-card">
    <div class="form-wrap">
      <div class="form-group text-center">
        <h2 class="mx-auto">DTM connect Okinawa</h2>
        <p class="text-secondary">沖縄に特化したDTMコミュニティ。沖縄で活動するアーティストや沖縄にゆかりのある方のご参加お待ちしております。DAW/プラグインのトラブル、ライヴ活用方法、レコーディング/Mixing Tips、イベントからお仕事のご依頼、音源の宣伝などご活用下さい！敬意のあるコミュニケーションをお願いします。初心者や今から音楽を始めたい方、お仕事でスタジオ経営しているプロまで幅広く歓迎致します</p>
      </div>
      <%= form_with scope: resource, as: resource_name, url: registration_path(resource_name), local: true do |f| %>
        <%if @sns_id.present? %>
          <%= hidden_field_tag :sns_auth, true %>
        <% else %>
          <div class="field">
            <%= f.label :password %>
            <% @minimum_password_length %>
            <em>(<%= @minimum_password_length %> characters minimum)</em>
            <br />
            <%= f.password_field :password, autocomplete: "new-password" %>
          </div>
          <div class="field">
            <%= f.label :password_confirmation %><br />
            <%= f.password_field :password_confirmation, autocomplete: "new-password" %>
          </div>
        <% end %>
        <div class="form-group">
          <%= f.email_field :email, autofocus: true, placeholder: "メールアドレス", class: "form-control" %>
        </div>
        <div class="form-group">
          <%= f.text_field :name, autofocus: true, placeholder: "フルネーム", class: "form-control" %>
        </div>
        <div class="form-group">
          <%= f.text_field :username, autofocus: true, placeholder: "ユーザーネーム", class: "form-control" %>
        </div>
        <div class="form-group">
          <%= f.password_field :password, autocomplete: "off", placeholder: "パスワード", class: "form-control" %>
        </div>
        <div class="form-group">
          <%= f.password_field :password_confirmation, autocomplete: "off", placeholder: "パスワードの確認", class: "form-control" %>
        </div>
        <div class="actions">
          <%= f.submit "登録する", class: "btn btn-primary w-100" %>
        </div>
      <% end %>
      <br>
      <p class="devise-link">
        アカウントをお持ちですか？
        <%= link_to "ログインする", new_user_session_path %>
      </p>
    </div>
  </div>
</div>
