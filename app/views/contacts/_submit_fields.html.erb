<div>
  <%= form.label :name %> (任意)
  <%= form.text_field :contact_name %>
</div>
<div>
  <%= form.label :email %> (必須)
  <%= form.email_field :contact_email, required: true, pattern: "^[a-z0-9._%+-]+@[a-z0-9.-]+\\.[a-z]+$" %>
</div>
<div>
  <%= form.label :content %> (必須)
  <%= form.text_area :content, required: true %>
</div>
